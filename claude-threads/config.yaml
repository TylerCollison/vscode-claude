version: 2
workingDir: ${DEFAULT_WORKSPACE}
chrome: ${THREADS_CHROME}
worktreeMode: ${THREADS_WORKTREE_MODE}
platforms:
  - id: mattermost
    type: mattermost
    displayName: Mattermost
    url: ${MM_ADDRESS}
    token: ${MM_TOKEN}
    channelId: ${MM_CHANNEL_ID}
    botName: ${MM_BOT_NAME}
    allowedUsers: []
    skipPermissions: ${THREADS_SKIP_PERMISSIONS}
